node_modules/
# dist/ is intentionally committed — the web-server binary serves it directly
# so Windows users don't need to install npm. Rebuild with `npm run build`.
src-tauri/target/
.env
*.local
.DS_Store
Thumbs.db
